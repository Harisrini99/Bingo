package com.example.bingo;

public class dummi {

              /*for(HashMap<String,Integer> map : mapArr) {
                        for (int k = 1; k <= 5; k++)
                        {
                            int t = map.get(k + "");
                            String idt = "b" + map.get(k + "");
                            if (t != 11 && t != 15 && t != 33 && t != 51 && t != 55)
                            {
                                int idintt = getActivity().getResources().getIdentifier(idt, "id", getActivity().getPackageName());
                                Button but = (Button) getActivity().findViewById(idintt);
                                boolean isAlreadyStricked = but.isClickable();
                                if (map.get("type") == 1 && isAlreadyStricked == true ) {
                                    final Drawable drawable = ContextCompat.getDrawable(getContext(), R.drawable.horizontal_line);
                                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
                                        but.setForeground(drawable);
                                    }
                                } else if (map.get("type") == 2 ) {
                                    final Drawable drawable = ContextCompat.getDrawable(getContext(), R.drawable.vertical_line);
                                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
                                        but.setForeground(drawable);
                                    }
                                } else if (map.get("type") == 3) {
                                    final Drawable drawable = ContextCompat.getDrawable(getContext(), R.drawable.right_slash);
                                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
                                        but.setForeground(drawable);
                                    }
                                } else if (map.get("type") == 4) {
                                    final Drawable drawable = ContextCompat.getDrawable(getContext(), R.drawable.left_slash);
                                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
                                        but.setForeground(drawable);
                                    }
                                }
                                 but.setClickable(false);
                            }
                        }
                    }
        */
}
